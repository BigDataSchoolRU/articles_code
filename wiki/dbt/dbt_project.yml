# dbt-core 1.12.3, прогнано на стенде 2026-08-28
name: 'dbt_demo'
version: '1.0.0'
config-version: 2

profile: 'dbt_demo'

model-paths: ["models"]
clean-targets: ["target", "dbt_packages"]

models:
  dbt_demo:
    staging:
      +materialized: view
    marts:
      +materialized: table
